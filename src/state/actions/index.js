import fetchApiData from './a_api-data'
import { setResizeState, fontsLoaded, hasTouch, setCurrentPixel, setScrollDirection, setDocHeight  } from './a_window-state'
import { setHeaderState, setMenuState, setModalState } from './a_ui-state'
import { setPageCount, setRoute, ageVerification } from './a_content-state'

export {
  fetchApiData,
  fetchInstaData,
  setResizeState,
  fontsLoaded,
  hasTouch,
  setCurrentPixel,
  setScrollDirection,
  setDocHeight,
  setPageCount,
  setRoute,
  setHeaderState,
  setMenuState,
  setModalState,
  ageVerification,
}